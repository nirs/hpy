"""
יבוא של gtk, שימוש בספריה גרפית 

באדיבות קובי זמיר
"""

# יבא ספריה גרפית חיצונית
יבא pygtk
יבא gtk

# הגדר שיגרה שתקרא עבור ארוע של לחיצה על הכפתור
הגדר לחיצה(רכיב, נתון=None):
    הדפס "שלום עולם"

# הגדר חלון ראשי לישום
חלון=gtk.Window(gtk.WINDOW_TOPLEVEL)
חלון.connect("delete_event", gtk.main_quit)

# הגדר כפתור
כפתור=gtk.Button("שלום עולם")
כפתור.connect("clicked",לחיצה,None)
חלון.add(כפתור)

# הראה את החלון והרץ את הלולאה הראשית של הישום
חלון.show_all()
gtk.main()
